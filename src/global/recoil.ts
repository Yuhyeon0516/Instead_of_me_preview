import { atom } from "recoil";

export const CategoryState = atom({
    default: "",
    key: "Category",
});

export const DetailCategoryState = atom({
    default: "",
    key: "DetailCategory",
});
