export interface ICategoryItem {
    title: string;
}

export interface IMarketItem {
    title: string;
    score: string;
    reviewCount: string;
    distance: string;
    deliveryPrice: string;
}
